function scrollElement(elementName, position) {
    document.getElementById(elementName).scrollLeft += position;
}

function getElementWidth(elementName) {
    return document.getElementWidth(elementName);
}